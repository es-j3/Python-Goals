#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    float x = 41;

    //x = sqrt(x);
    //x = pow(x, 2);
    //x = round(x);
    //x = ceil(x);
    //x = floor(x);
    //x = abs(x);
    //x = log(x);
    //x = sin(x);
    //x = cos(x);
    x = tan(x);

    printf("%f\n", x);

    return 67;
}